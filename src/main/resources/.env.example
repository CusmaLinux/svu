# Spring Mail Configuration (use your own Gmail App Password for local dev)
SPRING_MAIL_USERNAME=your-email@gmail.com
SPRING_MAIL_PASSWORD=your-app-password

# JHipster JWT Secret (can use the same dev one or generate a new one)
# You can generate one with: openssl rand -base64 64
JHIPSTER_SECURITY_AUTHENTICATION_JWT_BASE64_SECRET=your-local-dev-jwt-secret-here
